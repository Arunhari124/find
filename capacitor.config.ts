const config = {
  appId: 'com.find.app',
  appName: 'Find',
  webDir: 'public', // leave it, but not used
  server: {
    url: 'https://find-zeta-coral.vercel.app/',
    cleartext: true,
    androidScheme: 'https'
  }
};

export default config;